name: 🚀 Deploy website
on:
  push:
    branches: [ "main" ]
jobs:
  deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 📩 Get latest code
        uses: actions/checkout@v3

      - name: npm install && build
        run: cd wp-app/wp-content/themes/gkb-theme/ && npm install && gulp build

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.3.3
        with:
          server: 178.172.164.22
          username: ftpgkb@gazkotelbel.com
          password: Ufprjn`k,tk
          local-dir: ./wp-app/
          server-dir: "./public_html/"
